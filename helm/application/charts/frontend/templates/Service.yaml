apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.service.name }}
  namespace: {{ .Values.sharedNamespace }}
spec:
  selector:
    app: {{ .Values.frontend.deployment.podName }}
  ports:
    - protocol: TCP
      port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.deployment.containerPort }}
